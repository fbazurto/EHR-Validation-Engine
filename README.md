# EHR-Validation-Engine
A middleware validation layer that intercepts clinical data at the point of entry, flagging anomalies, inconsistencies, and probable errors before they are committed to the EHR record.
